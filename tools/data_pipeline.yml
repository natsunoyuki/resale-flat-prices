# Configurations for data_pipeline.py.

# Directory containing the raw CSV files.
csv_data_dir: "data/ResaleFlatPrices/"

# Directory containing the processed data (e.g. geocoded addresses etc.)
processed_data_dir: "data/processed_data/"

# Pre-existing JSON file containing the geocoded addresses.
geocoded_addresses_json_file: "geocoded_addresses.json"

# Output file name of the merged processed data.
output_csv_file: "resale-flat-prices.csv.zip"